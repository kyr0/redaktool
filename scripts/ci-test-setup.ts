
/** !! this code is loaded by vitest before all test code and
 *  is executed at runtime in test only !! */

// called once before each test suite
beforeAll(async () => {
  // for demonstration purposes only
  //await pause(0);
});

// called once before each test
beforeEach(async () => {
  // for demonstration purposes only
  //await pause(0);
});
